{
  "name": "friends",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --open",
    "build": "tsc && vite build",
    "preview": "tsc && vite build && vite preview --host --open"
  },
  "typeRoots": [
    "node_modules/@types",
    "src/@types"
  ],
  "devDependencies": {
    "@babylonjs/addons": "^8.8.0",
    "@babylonjs/core": "^8.8.0",
    "typescript": "^5.3.3",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "babylonjs": "^6.38.0"
  }
}
